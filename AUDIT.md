# Python 365 审计总览

> 这份文档把散在 5 份报告里的信息串成一条线：**攻击面怎么演进、每个阶段补了什么、最后剩下什么。**
> 想深挖某一阶段 → 看对应报告；想自己复现 → 见文末「一键复现」。

## 0. 审计对象与方法

**对象**：`python365` —— 用 [dowhen](https://github.com/gaogaotiantian/dowhen) 在解释器启动时
把付费墙挂到语言与标准库上的订阅制 Python 运行时。纯娱乐/教学实现，不修改 CPython 与用户代码。

**方法**：每个阶段换一批攻击面；脚本**必须自己打印 `✅ HACKED` 才算攻破**
（避免"崩了就算赢"的假阳性 —— 这条规则本身后来抓到过 3 次错误判定，见第 6 节）。

**威胁模型分三条线**，这个划分贯穿全程，也是理解"为什么有些漏洞修不掉"的钥匙：

| 线 | 攻击者能做什么 | 典型漏洞 |
|---|---|---|
| **进程内** | 在同一进程里跑代码：patch 模块属性、借用对象、伪造身份 | 五轮里绝大多数 |
| **服务端** | 能向厂商授权服务端发 HTTP（租约架构引入的新面） | `/renew` 不验签 |
| **分发/运行环境** | 控制解释器启动方式、能读同机文件 | `-S` 启动、可读 `.pyz`、私钥同机 |

## 1. 五个阶段

| 阶段 | 谁做的 | 战绩（修复前 → 修复后） | 报告 |
|---|---|---|---|
| **自审 + 修复** | 我 | 12 项攻击，**11 攻破** → 全封堵 | `exploits/REPORT.md` |
| **外部审计·第三轮** + 修复 | 外部 | 14 项，**13 攻破** → 13 守住 / 1 攻破¹ | `exploits/REPORT_ROUND3.md` |
| **外部审计·第四轮** + 修复 | 外部 | 10 项，**8 攻破 / 1 自伤** → 9 守住 / 1 攻破² | `exploits/REPORT_ROUND4.md` |
| **租约架构实验** | 我 | 6 个场景：3 条残留从"不可修"变为"可修" | `exploits/REPORT_ARCH_LEASE.md` |
| **外部审计·第五轮** + 修复 | 外部 | 10 项，**9 攻破 / 1 误伤** → 9 守住 / 1 攻破³ | `exploits/REPORT_ROUND5.md` |
| **外部审计·第六轮** + 修复 | 外部 | 5 项，**5 攻破** → 5 守住 / 0 攻破 | `exploits/REPORT_ROUND6.md` |
| **外部审计·第七轮** + 修复 | 外部 | 6 项，**6 攻破** → 4 守住 / 1 缓解¹ / 1 边界 | `exploits/REPORT_ROUND7.md` |
| **外部审计·第八轮** + 修复 | 外部 | 4 项（含 1 项自伤），**4 攻破** → 3 守住 / 1 需 TLS | `exploits/REPORT_ROUND8.md` |
| **自签 TLS 实验** | 我 | 3 个场景：明文 🔴 劫持 / TLS 🟢 抓不到 / 中间人 🟢 握手被拒 | `exploits/REPORT_ARCH_TLS.md` |

¹ 明文通道上的 bearer 租约（缓解=绑定来源地址，真修法=TLS）

¹ 可读 `.pyz`（已按项目决定移出范围） ² 私钥与客户端同机（架构边界） ³ 同上，见第 4 节

**规模演进**

| 指标 | 起点 | 现在 |
|---|---|---|
| 付费墙（被挂钩子的函数入口） | 4870 | **6444**（子模块铺墙 +8000，剔除继承方法与抽象基类 −6000+） |
| 客户端的身份判据 | `co_filename` 字符串 | 代码对象身份 + 模块 `__dict__`×`__file__` 双重校验 |
| 产品 + 厂商代码 | 单文件 1090 行 | 9 个模块 + 3 个厂商工具，**2565 行** |
| 攻击脚本 | 0 | **约 50 个**（四套件 45 项 + 5 个独立脚本） |

## 2. 主线：判据换了五次，「借」这个动作一直成立

这是整个项目最值得记的一条线。每一轮我都把"怎么判断这一帧是谁"换了个判据，
而对手总能从环境里**借**一个属性来冒充：

| 阶段 | 当时用的判据 | 对手怎么借 | 结果 |
|---|---|---|---|
| 第一轮 | `frame.f_code.co_filename` | `compile(src, "/usr/lib/python3.12/legit.py")` | 一行白嫖 |
| 第三轮 | 帧的 `__name__` 字符串 | 模块里写一行 `__name__ = "python365.fake"` | 一行白嫖 |
| 第四轮 | `_INTERNAL_CODE_IDS`（模块级可变集合） | 换成一个 `__contains__` 恒真的对象 | 一行白嫖 |
| 第五轮 | `id(frame.f_globals)`（模块 `__dict__` 身份） | `types.FunctionType(compile(PROG, "<prog>"), SITE.__dict__)` | 一行白嫖 |
| 第五轮·服务端 | —— | 服务端把"客户端报上来的载荷"直接当可信输入 | 零凭据换到签名租约 |
| **第六轮** | `id(f_globals)` **且** `co_filename == __file__` | **同一招 + 多填一个 `compile()` 参数**（`co_filename` 是第一轮就报废的字段，却被请回来当加固） | 一行白嫖 |

→ 第七轮的做法：**模块级帧只比"该模块的模块级 code object 本身"**。代码对象没法借 ——
要让它命中，只能真去执行我们的模块顶层，那只会把墙装得更牢。

**教训**：判据不能取自"环境里可以借用的属性"，只能取自**不可借用的身份**。
四个属性、四种伪造，最后的答案是"代码对象身份 + 双重校验"，
以及一条更硬的：**凡是在客户端能算出来的结论，都不算凭据。**

## 3. 全部防线（按机制）

| 机制 | 治好了什么 | 出处 |
|---|---|---|
| **代码对象身份**（启动期收集自己人的 code id） | 改名 / 伪造 `__name__` / 借 `__dict__` | R3-1、R4-4、R5-1 |
| **模块 `__dict__` × `__file__` 双重校验** | 借内部模块的命名空间当 globals | R5-1 |
| **只有顶层帧是用户代码**（`f_back is None`）；`__main__` 永不"可信" | `python3 - < x.py` / `-c` + `exec` 白嫖 | R4-1 |
| **执法动作启动期绑定**（默认参数 / 闭包） | patch `on_tamper` / `integrity_check` / `_EXIT` / `paywall` | R3-4、R4-5、R5-4 |
| **状态进闭包 + 单调影子值** | 改额度、回退用量计数器、双写影子值 | R3-3、R4-3 |
| **账户只导出函数**（无 `state`/`busy_state`/`bind_checker`） | 就地改写内部状态 | R4-3 |
| **原对象只进闭包 cell**（不挂 `__python365_shim__`） | 从包装层取回未受保护的 C 函数 | R4-2 |
| **模块墙 + 实现子模块铺墙 + 惰性补墙**（挂 `_find_and_load`） | 绕过父模块直接调实现层、后加载子模块裸奔 | R3-8、R4-7 |
| **抽象基类不铺墙 + 类只展开自己定义的方法** | `os.environ.get()` 被收「基础版」（误伤） | R5-7 |
| **C 模块包装层**（函数 + 类型入口） | 用 `_csv` / `_json` / `_random` 白嫖 | R3-6、R3-9 |
| **import 闸门**（`_find_and_load` + `__import__` + `meta_path`） | C 模块的越狱通道 | R3-9 |
| **内核 `RLIMIT_CPU`**（soft==hard）+ fork 收紧 | 零事件纯 C 运算、fork 逃逸 | R2-7、R2-12 |
| **双源时钟 + 高水位** | 拨回系统时钟复活过期许可证 | 缺口 B |
| **授权决策启动期固化** | 运行期改授权表 | 缺口 A |
| **RSA 签名许可证 + 到期日** | 伪造激活码 | R2-10 |
| **续费要凭签名令牌**（`apply_topup`） | 官方 API 自助充值 | R3-6 |
| **租约架构**：短周期令牌 + 设备绑定 + 吊销 + 独立闸门线程 | 换机器、吊销、离线窗口有上限 | 租约实验 |
| **服务端验签 + 管理接口认证 + 不存凭据原文** | 纯协议零凭据拿租约、状态文件泄露客户凭据 | R5-5/6/9 |
| **私钥移出客户端目录树**（`~/.python365-vendor/`） | 从仓库/客户端树读到私钥 | R3-12 |
| **反篡改**：冗余监控器 + 探针自测 + 蜜罐 + `del sys.monitoring` | 拆钩子、关监控、改计量表 | R2-1/2/8 |

## 4. 当前状态：剩下 1 条

| 条目 | 状态 | 为什么 / 靠什么 |
|---|---|---|
| **私钥与客户端同机同 uid**（R5-3） | 🔴 **仍可攻破** | 这是**已记录的架构边界**，不是"没修"：隔离需要独立主机 / 独立 uid / HSM。本沙箱是 proot(fake-root)，连权限语义都测不出来（实测 nobody 仍能读 0600 root 的文件） |
| `os.urandom` / `ctypes` / 指针通道 | 🟡 靠内核 | dowhen 只能 hook Python 代码对象；实测免费版仍有 10/12 个 C 通道能力白送 |
| 零事件的纯 C 运算 | 🟢 有上限 | 逃费上限 = 内核 `RLIMIT_CPU` = 6 秒/进程 |
| 机器指纹是客户端自报的 | 🟡 靠硬件 | 挡得住拷贝租约，挡不住改一行 `fingerprint()`；真绑定需 TPM/飞地或服务端侧信号 |
| 进程内反篡改只是抬高门槛 | 🟡 | 不是形成证明；上了租约之后，全 patch 的攻击者也被限制在"租约 TTL + 宽限期"窗口内 |
| 无在线吊销的旧许可证 | 🟢 已解决 | 租约模式下靠服务端仲裁 |

**可读 `.pyz`** 与 **`python3 -S` 启动** 已按项目决定**移出威胁模型**（属"分发"，不计入）。

## 5. 一键复现

```bash
cd python365/exploits
./run_suite.sh          # 自审套件    12 项 → 12/12 封堵
bash r3/run_suite.sh    # 第三轮套件  13 项 → 13 守住 / 0 攻破
bash r4/run_suite.sh    # 第四轮套件  10 项 → 10 守住 / 0 攻破 / 0 自伤
bash r5/run_suite.sh    # 第五轮套件  10 项 →  9 守住 / 1 攻破（R5-3，前提已记录）

cd ../demos
python3 lease_lab.py            # 租约六场景：激活 / 换机停机 / 吊销停机 / 断网停机 / 冒用指纹 / 复测老洞
python3 uid_separation_lab.py   # "私钥到底谁能用"：藏起来 → 客户端照常、厂商签发全瘫
python3 demo_app_365.py         # 免费版撞 16 道墙（需 PYTHONPATH=/path/to/python365）
```

## 6. 跨阶段工程教训

1. **懒加载是天然的漏洞源**（踩了三次）：
   `dowhen.handler` 让 4870 道墙集体哑火 → `_lease` 让监控器误收自己人的税 →
   `_gcd_import` **从来没被调用过**（我第三轮"import 闸门三层"里那层一直是死代码，
   当时只验证了"结果被拦住"，没逐层验证）。→ **多层防线必须逐层验。**
2. **攻击脚本的判定必须忠实**（抓了三次假阳性）：x10 被语法包挡住、x18 打印路径自己撞墙、
   r5_10 无条件打印 `HACKED`。→ 否则"被修复"会被脚本自己掩盖。
3. **栈深度假设很脆**：`user_frame()` 的 `_getframe(2)` 是按 dowhen 回调链算的，
   加一层包装就错位（一次把"调用者的调用者"也算成计费调用，一次让 C 包装层把自己当用户帧）。
4. **热路径上不能有 I/O**：把"读高水位文件"放进 50ms 快循环 → 启动期 CPU 从 0.38s 涨到 1.95s，
   免费版 3 秒额度被吃掉一半。
5. **顺序即正确性**：蜜罐必须在 `del sys.monitoring` **之前**造好（dowhen 每次回调都要读
   `sys.monitoring.DISABLE`）；拆 monitoring API 必须在所有装配**之后**（装每道墙都要读它）。
6. **"再加一个检查"是错的修复模式**：前几轮的修法都是补检查，而**检查所依赖的前提**本身可改。
   正确模式是**换掉输入** —— 身份换代码对象、状态进闭包、结论搬到服务端。
7. **开发环境（这个沙箱）的两条坑**：跨目录 `mv` 会得到一堆悬空软链（本项目因此丢过一次 git 历史，
   用 `cp -r` + `rm -rf`）；proot(fake-root) 让文件权限语义测不出来；**命令上限 15 秒**，
   所以慢的验证要拆成逐条跑。
8. **补规则要检查它依赖的不变量**：第七轮把"遇到内部帧就放行"改成"跳过继续找"，
   前提是"链上总有 level-0 帧" —— 而**新线程的根帧没有调用者**，这个前提直接不成立
   （第八轮 R8-01，一行代码打穿全部墙）。**改判定规则之前，先写出它默认成立的前提，再逐条验。**
9. **安全修复也要算成本**：同一次修改让每次调用都走完整条栈，免费版启动 CPU 1.61s → 5.01s，
   内核上限 6s —— 用户拿到的是 SIGKILL 而不是付费墙（第八轮 R8-04，自伤）。
   修完安全要**量一遍性能**，否则"更安全"会变成"更不可用"。
10. **指纹要细到"对象身份"，不能只数个数**：R3-7 的注册表指纹记的是
   `len(Instrumenter().handlers)`（有多少个 code object 被挂钩子），而
   `handlers[code]["start"]` 是**可以原地清空的列表** —— 键还在、长度不变、
   **7117 道墙静默消失**（第 21 号攻击，由"dowhen 还有别的 API 吗"这个问题引出来的）。
   现在的快照细到"每个 code 的每个 kind 挂着哪些 handler 对象 id"。
   → 由此还要记得：**接入的接口越大，可达面越大** —— dowhen 的 `handlers` 是可写数据结构，
   它既是我们的执行机制，也是攻击者的入口。
11. **有些残留的正确答案不是"补规则"，而是"换通道"**：明文 bearer 凭据被补了两次规则
    （轮换令牌 → 绑定来源地址），两次都被下一条路线绕开；直到上 TLS + cert pinning
    （`REPORT_ARCH_TLS.md`）。每次补规则前先问：**我保护的是判断，还是被判断的东西？**

## 7. 结论：三根支柱

五轮下来，攻击面从"绕过检查" → "改检查的前提" → "改检查依赖的对象" → "改信任模型的输入"
→ 最后搬到服务端。每一轮修完，对手都往更外面退一层，剩下的全部指向**进程之外**：

| 支柱 | 治好了什么 | 本项目的状态 |
|---|---|---|
| **控制运行时** | 零事件 C 运算、fork 逃逸 | ✅ 内核 `RLIMIT_CPU`（逃费上限 6 秒/进程） |
| **控制分发** | 私钥泄露、`.pyz` 可读、`-S` 启动 | 🟡 私钥已搬出客户端树；真隔离需独立主机/HSM |
| **控制数据** | 授权判定、吊销、机器绑定 | ✅ 租约架构（服务端仲裁）；机器指纹仍靠客户端自报 |

> **进程内的东西决定"门槛有多高"，进程外的东西决定"能不能证明"。**
>
> 门是"锁"上的，不是"砌墙"上的 —— 而锁芯和钥匙，最终都不该在对手手里。
