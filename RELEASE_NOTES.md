# Python 365 v1.0.0

把 CPython 改造成**订阅制运行时**：标准库按模块卖、语言特性按版本卖、CPU 时间按秒计费。
免费版带广告、随机数被降级成常量、每 5 万次函数调用或 3 秒 CPU 时间断供一次。

> ⚠️ **整活 / 教学项目，不是真产品。**
> 它一半是产品，一半是"它为什么挡不住"的论证材料 —— 这个仓库同时是一部十一轮红队审计的完整记录。

## 这一版有什么

**客户端**（`python365/`，9 个模块）

- 用 [dowhen](https://github.com/gaogaotiantian/dowhen) 把墙挂到标准库与语言特性上：
  **7112 道付费墙**（覆盖 52 个目标模块/类）+ 30 个 C 包装层
- **双层计费**：函数调用次数 + CPU 时间。后者是为了抓 C 层运算 ——
  `sum(range(10**8))` 只产生 1 次函数调用、却烧掉 1.7 秒 CPU，第 1 层完全看不见它
- **随机数降级**：免费版的 `random` 恒为常数（不是拒绝服务，是"给你个假随机"）
- **import 闸门**：C 实现的标准库（`math` / `sqlite3` / `hashlib` …）只能在 import 那道门收税

**授权**（`vendor/` + `_license.py` / `_lease.py`）

- 离线 RSA 签名许可证；在线**租约**：短周期令牌、设备绑定、服务端吊销、TLS + cert pinning
- 客户端只含**公钥**与厂商 CA —— 签名能力只在厂商侧

**审计**（`exploits/` + `AUDIT.md`）

- **十一轮红队审计**的完整记录：4 套回归套件 + 约 65 个**可用**的绕过脚本 + 6 份报告
- 每一轮都留下了修法与踩坑（包括其中一次**自伤**：某次修法让免费版启动 CPU 从 1.6 秒涨到 5 秒，
  用户拿到的是内核 `SIGKILL` 而不是付费墙）
- 结论：**进程内决定门槛，进程外决定证明**

## 快速上手

```bash
git clone https://github.com/fangyaota/python365 && cd python365

PYTHONPATH=$PWD python3 demos/demo_app_365.py    # 免费版撞 16 道墙
PYTHONPATH=$PWD python3 demos/lease_lab.py       # 在线租约六场景（激活/换机/吊销/断网/冒用指纹/复测老洞）
python3 demos/tls_lab.py observe                 # 明文通道上的令牌劫持（对照 observe-tls 看 TLS 为什么是正解）
python3 tools/build_release.py                   # 生成客户交付版（14 个文件 / 60 KB）
bash exploits/run_suite.sh                       # 跑一遍自审套件（12 项）
```

## 已知限制（诚实清单）

- **拦得住的是"没打算绕"的人**：执法运行在你自己的进程里，进程内的一切都可被改写 ——
  这是"进程内计费"这一形态的上限，不是配置问题
- **C 通道**：`os.urandom` / `ctypes` 等不经过 Python 层，无法计费（实测免费版仍能白拿 10/12 个 C 能力）
- **零事件的纯 C 运算**：只能靠内核 `RLIMIT_CPU`（约 6 秒/进程），超支时你收到的是 `Killed`
- **机器指纹由客户端上报**：设备绑定只能挡住"老实拷贝"
- 完整清单见 README 的「已知限制」与 `AUDIT.md` 的「仍然修不掉的」

## 系统要求

**Python 3.12 及以上**（依赖 PEP 669 的 `sys.monitoring`）。

## 声明

MIT。仓库里的「定价」「许可证」「厂商授权服务端」都是演示道具；
`exploits/` 里的攻击脚本**只针对本项目自己的代码**，用途是把"本地计费"这类系统的边界演示清楚 ——
本项目十一轮审计的结论恰恰是"它不能当真正的 DRM"。
